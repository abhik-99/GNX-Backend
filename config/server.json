{
    "host": "${process.env.HOST}",
    "port": "${process.env.PORT || 3000}",
    "production": true,
    "proxy": {
      "enabled": false
    },
    "cron": {
      "enabled": false
    },
    "admin": {
      "autoOpen": false,
      "auth":{
          "secret": "${process.env.ADMIN_JWT_SECRET}"
      }
    }
}