{
    "host": "${process.env.HOST || 0.0.0.0}",
    "port": "${process.env.PORT || 1337}",
    "production": true,
    "proxy": {
      "enabled": false
    },
    "cron": {
      "enabled": false
    },
    "admin": {
      "autoOpen": false,
      "auth":{
          "secret": "${process.env.ADMIN_JWT_SECRET}"
      }
    }
}